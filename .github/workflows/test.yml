name: Hola Mundo1

on:
  push:
    branches:
      - master
      - Qa

  pull_request: 
    branches:
      - master
      - Qa

jobs:
  hola:
    runs-on: ubuntu-latest

    steps:
    - name: Hola Mundo2
      run: echo "Hola Mundo3"

    - name: touch file
      run: touch hola.txt

    - name: Set text
      run: echo "hola mundo 4" >> hola.txt

    - name: Read file
      run: cat hola.txt

  other:
    runs-on: ubuntu-latest
    needs: [hola]

    steps:
    - name: LS
      run: ls -al